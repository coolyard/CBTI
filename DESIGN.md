# DESIGN · 设计宪法（宪法层）

> 本项目没有设计稿——本文件 + [specs/40-design-system.md](specs/40-design-system.md) 就是设计稿。
> 可执行的令牌/组件视觉规范以 specs/40、specs/55、specs/70 为准；本文件只定哲学与边界。
> 维护规则：由 AI Agent 依据 specs/ 生成与更新。

## 1. 设计哲学：「灵魂漫研所」

傻雕漫画 + 贴纸质感。用户 18–35 岁，来这不是做题的，是来**笑着截图发朋友圈的**。
因此设计优先级：**传播欲 > 情绪感 > 信息密度**。

四个不可妥协的视觉基因：

1. **粗描边**：一切卡片/按钮 2–3px ink 描边
2. **硬阴影**：`4px 4px 0 ink`，禁止柔模糊阴影
3. **高饱和**：paper 米黄底 + 灵魂紫/梗黄/扎心粉/薄荷绿
4. **弹跳感**：动效统一回弹曲线 `cubic-bezier(0.34, 1.56, 0.64, 1)`，按压即"贴纸被按扁"

## 2. 反面清单（AI 生成 UI 时的高压线）

- ❌ 毛玻璃 / 蓝紫渐变 / 彩虹渐变装饰
- ❌ 卡片套卡片、顶部/左侧色条装饰
- ❌ 无意义的滚动触发淡入、hover 放大
- ❌ 居中 Hero + 双 CTA + 三栏特性卡的模板构图
- ❌ 图标库/图标字体（一律用 emoji）
- ❌ 中文斜体（强调用加粗/底色高亮）

完整清单与令牌表见 specs/40。

## 3. 关键视觉决策

| 决策 | 结论 | 出处 |
|------|------|------|
| 设计令牌单一来源 | specs/40，同步到 `uno.config.ts` + `uni.scss` | specs/10 §7 |
| 雷达图 | 自研 Canvas 2D 五边形，双线对比（用户紫实线面 + 角色黄虚线） | specs/55 |
| 海报 | Canvas 2D 统一绘制 750×1334，禁 html2canvas | specs/50-pages/poster.md |
| 立绘 | 傻雕漫画风离线预生成，图文分离（台词由代码渲染气泡） | specs/70 |
| 文案语气 | SBTI 风格：像损友不像咨询师 | specs/60 |

## 4. 新增 UI 的决策流程

```
要加新 UI？ → 先查 specs/40 令牌能否组合出来
  → 能：直接用，禁止发明新颜色/新圆角
  → 不能：在 specs/40 中先登记新令牌，再使用
```

任何"这个场景特殊，就这一次用个新样式"的念头，都是设计系统崩坏的开始。
